---
description: Codex adapter for the full sprint loop.
argument-hint: <feature description>
---

# sprint

Do not edit this adapter for workflow changes. Edit
`.claude/commands/sprint.md` instead.

Run the contract in `.claude/commands/sprint.md`.

Use the phase adapters in `commands/` only as Codex entry points. Preserve the
approval gates and write all artifacts under `.claude/state/sprints/`.

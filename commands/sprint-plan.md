---
description: Codex adapter for sprint planning.
argument-hint: [sprint-id]
---

# sprint-plan

Do not edit this adapter for workflow changes. Edit
`.claude/commands/sprint-plan.md` instead.

Run the contract in `.claude/commands/sprint-plan.md`.

Use `agents/pm.md` if sub-agents are available; otherwise perform the PM role in
the main loop with the same write boundary.
